<p id="notice"><%= notice %></p>

<h1>Lista de Professores</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Apelido</th>
      <th>Disciplina</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @professors.each do |professor| %>
      <tr>
        <td><%= professor.nome %></td>
        <td><%= professor.apelido %></td>
        <td><%= professor.disciplina %></td>
        <td><%= professor.email %></td>
        <td><%= link_to 'MOSTRAR', professor %></td>
        <td><%= link_to 'EDITAR', edit_professor_path(professor) %></td>
        <td><%= link_to 'DELETAR', professor, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Professor', new_professor_path %>
